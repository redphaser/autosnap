lenin = servers[0].id
print lenin
clear
lenin = cs.servers.get(servers[0].id)
print lenin
snapname = "autosnap-" + servers[0].id
print snapname
server.create_image(snapname)
lenin.create_image(snapname)
snapname = servers[0].name + "-" + servers[0].id + "-autosnap"
print snapname
lenin.create_image(snapname)
lenin.create_image(snapname)
history
